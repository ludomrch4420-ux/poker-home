{
  "name": "poker-home",
  "version": "1.0.0",
  "private": true,
  "description": "Texas Hold'em poker — Cloudflare Worker",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
